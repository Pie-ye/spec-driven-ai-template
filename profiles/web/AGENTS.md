# Web Profile Rules

- `package.json` and `pnpm-lock.yaml` are dependency sources of truth.
- Use pnpm scripts; do not introduce a second package manager.
- Follow the existing framework, component, accessibility, and browser support conventions.
- Keep generated bundles and local `.env` files out of Git.
