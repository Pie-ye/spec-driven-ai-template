# Python service example

Create `pyproject.toml` and `uv.lock`, enable `python`, then configure Ruff, pytest, and mypy/pyright as project dependencies. The profile adapter delegates setup and verification to uv.
