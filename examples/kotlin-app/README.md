# Kotlin app example

Keep `gradlew`, `gradle/wrapper/`, and `build.gradle.kts` in the application. Enable `kotlin`; the adapter never invokes a global Gradle binary. Android SDK and emulator setup remain host-specific.
