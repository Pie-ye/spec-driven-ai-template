# Qt desktop example

Keep `CMakeLists.txt`, `CMakePresets.json`, and test targets in the application. Enable `qt-cpp`; install the Qt SDK and a compatible compiler/ABI on the host or container. `doctor` reports missing Qt SDK configuration.
